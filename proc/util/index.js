
// mode :: strict
// --------------------------------------------------------------------------------------------
   "use strict";
// --------------------------------------------------------------------------------------------



// name :: Xmod
// --------------------------------------------------------------------------------------------
   Name
   ({
      Xmod:
      (
         function(path,resl,list,item)
         {
            path = process.path.self+'/xmod/init/';
            list = process.path.readdirSync(path);
            resl = {};

            for (item in list)
            {
               if (list.hasOwnProperty(item))
               { resl[(list[item].split('.')[0])] = require(path+list[item]); }
            }

            return resl;
         }()
      )
   });
// --------------------------------------------------------------------------------------------


   Name
   ({
      foo:'bar'.meta
      ({
         
      })
   });


/*
// mode :: strict
// --------------------------------------------------------------------------------------------
   "use strict";
// --------------------------------------------------------------------------------------------



// name :: Xmod
// --------------------------------------------------------------------------------------------
   Name
   ({
      Xmod:
      (
         function(resl)
         {
            resl = {};

            Path.readdir('/xmod').drop('index.js').each = function(k,v)
            {
               resl[(v.split('.')[0])] = require(Proc.path.self+'/umod/'+v);
            }

            return resl;
         }()
      )
   });
*/
